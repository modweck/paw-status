/** Number of days of slots to generate ahead of now. */
export const HORIZON_DAYS = 30;

/** Lookahead window (days) used for "next available" queries. */
export const NEXT_AVAIL_HORIZON_DAYS = 7;

/** Minutes a waitlist offer remains valid before it expires. */
export const OFFER_TTL_MINUTES = 30;

/** Minimum hours between now and the start of a bookable slot. */
export const DEFAULT_LEAD_TIME_HOURS = 2;
